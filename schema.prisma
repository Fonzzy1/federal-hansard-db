generator client {
    provider = "prisma-client-py"
}

datasource db {
    provider = "sqlite"
    url      = "file:./dev.db"
}

model Source {
    id   Int    @id @default(autoincrement())
    name String
}
